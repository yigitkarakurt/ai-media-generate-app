{
	"name": "ai-media-generate-app",
	"description": "Backend API for AI media generation platform. Cloudflare Workers + D1 + R2.",
	"private": true,
	"dependencies": {
		"aws4fetch": "^1.0.20",
		"hono": "4.11.1",
		"zod": "3.25.67"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.10.11",
		"@types/node": "24.10.1",
		"typescript": "5.9.3",
		"vitest": "3.2.4",
		"wrangler": "4.56.0"
	},
	"scripts": {
		"dev": "wrangler d1 migrations apply DB --local && wrangler dev",
		"deploy": "wrangler deploy",
		"predeploy": "wrangler d1 migrations apply DB --remote",
		"cf-typegen": "wrangler types",
		"test": "wrangler deploy --dry-run && npx vitest run --config tests/vitest.config.mts"
	}
}
